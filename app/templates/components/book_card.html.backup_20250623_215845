<!-- Book Card Component -->
<div class="bg-white rounded-lg shadow-md p-4 hover:shadow-lg transition-shadow">
    <a href="/book/{{ book.id }}" class="block">
        <!-- Book Cover -->
        <div class="bg-gray-200 h-48 rounded mb-3 flex items-center justify-center">
            {% if book.cover_image_url %}
                <img src="{{ book.cover_image_url }}" alt="{{ book.title }}" class="h-full object-cover rounded">
            {% else %}
                <span class="text-4xl">📚</span>
            {% endif %}
        </div>
        
        <!-- Book Info -->
        <h3 class="font-semibold text-lg mb-1 line-clamp-1">{{ book.title }}</h3>
        <p class="text-gray-600 text-sm mb-2">by {{ book.author }}</p>
        
        <!-- Rating -->
        <div class="flex items-center mb-2">
            <span class="text-yellow-500">
                {% for i in range(book.rating|int) %}⭐{% endfor %}
            </span>
            <span class="text-gray-500 text-sm ml-2">({{ book.total_reviews|default(0) }})</span>
        </div>
        
        <!-- Year -->
        <p class="text-gray-500 text-sm">{{ book.publication_year|default('Unknown') }}</p>
    </a>
    
    <!-- Save Button -->
    <button 
        class="mt-3 w-full bg-blue-100 text-blue-600 py-2 rounded hover:bg-blue-200 transition-colors"
        onclick="toggleSaveBook({{ book.id }})"
        id="save-btn-{{ book.id }}"
    >
        💾 Save Book
    </button>
</div>